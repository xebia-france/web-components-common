import styled from 'styled-components';
import {ContainerCommon} from "../../styles/common.styled";

export const Container = styled(ContainerCommon)``;

export const LinedCTA = styled.div`
    display : flex;
    flex-wrap : wrap;
`;